---
- hosts: appliance
  gather_facts: false
  tasks:
    - import_role:
        name: ~/collections/network

    - include_vars:
        file: test_vyos_interfaces_vars.yaml

    - include_tasks: "{{ item }}.yaml"
      loop:
        - merged
        - replaced
        - overridden
        - deleted
