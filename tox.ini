[flake8]
# These are ignored intentionally;
# please don't submit patches that solely correct them or enable them.
ignore = E501,E125,E129,E402
show-source = True
exclude = .venv,.tox,dist,doc,build,*.egg
max-line-length = 100
